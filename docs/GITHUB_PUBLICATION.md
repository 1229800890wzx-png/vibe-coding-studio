# GitHub 发布源码检查

目标仓库为 `1229800890wzx-png/vibe-coding-studio`，开发分支为 `codex/vibe-edu`。该分支包含教学小程序、后台、服务端、初始化与迁移脚本、原创课程素材和验收材料。运行数据库、备份、环境密码、依赖安装目录及构建产物保留在被 Git 忽略的本地目录。

2026-09-14 首次推送被 GitHub 密钥保护拒绝：固定上游的示例 SQL 含云服务凭据。处理方式是清理源码及未公开提交历史，未使用 GitHub 的放行链接，也未关闭密钥保护。

## 上游源码净化

- 移除八种方言脚本中 `infra_file_config` 和 `system_sms_channel` 的示例 INSERT，共 112 行；保留建表语句。原本地初始化的种子白名单已排除这些记录，因此不影响现有数据库或初始化行为。
- 将上游外部服务、设备集成测试和 HTTP 请求示例中的硬编码凭据改为环境变量或请求变量；这些示例需要使用者自行配置。
- 刷新数据库和迁移包的源码 SHA256 清单。业务建表 SQL、八个 Liquibase 迁移 SQL、XML 和 POM 均未改变。
- 固定上游提交用于标识来源；本仓库属于经过上述清理及教育业务修改的二次开发源码，不能当作上游原样镜像。三个项目的根许可证保留。

## 检查边界

发布准备包含 Git 路径检查、文件大小检查、已知本地凭据匹配、Gitleaks 扫描和 GitHub 原生推送保护。Gitleaks 的通用 key 规则也会标记公开算法标识、短信模板编号和上游加密示例；需按具体用途核实，不能把扫描结果数量直接当成真实凭据数量。

发布源码不等于部署生产服务。正式微信登录、商户支付退款、COS、真实课程资料和真机验收边界见 [运行手册](OPERATIONS.md) 与 [实施差距](IMPLEMENTATION_GAPS.md)。
