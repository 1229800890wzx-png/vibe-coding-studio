# 朋友前端与统一后台接入验收

日期：2026-09-14。用户要求：合并朋友 PR #1，前端按朋友版本，将统一后台整理为新的 PR。

PR #1 已合入 `master`，合并提交为 `3532c0757ecc425ef2bac30118c12b5a11440fe0`。本次整合分支为 `codex/unified-platform`，合入上述 master 历史，并保留此前 `fc320d65` 的统一后端、管理后台、数据库迁移及小程序。

## 前端与运行入口

- 原橙色首页、CSS、教学方法、导师、作品、课程筛选搜索、列表和详情保持朋友版本。
- 课程读取统一 `website-offering/list`，`slug` 用于 `/courses/:id` 导航，数字展示配置 ID 与可选真实 `courseId` 分开。
- 咨询保留称呼、手机/邮箱、经验、兴趣、留言和授权勾选；成功以统一后端回执为准，包含授权换版恢复、按内容去重重试及提交期间禁用控件。
- 官网课程与咨询管理由 `apps/admin` 承接；原 `platform` 独立服务、自动建旧表、管理员初始化和旧后台 overlays 退役。保留许可证、上游来源与历史说明，没有运行或清理旧数据库及本机服务。
- 新默认内容清单采用朋友的三方向文案。生产导入仍只创建缺失 slug，不覆盖运营记录；本次仅在独立恢复库更新原样的测试默认记录。

## 本次实际验证

| 检查 | 结果 |
| --- | --- |
| 官网 TypeScript 检查与 Vite 构建 | 通过；CSS 构建文件与朋友基线相同 |
| 网关身份头、路由、请求大小与异常处理测试 | 通过 |
| Liquibase 资产检查 | 通过；001–008 冻结，009/010 与统一版本一致 |
| 独立适配代码审查 | PASS，无 P1/P2 |
| 12 组视觉检查 | 桌面/移动端 × 首页、课程列表、详情、教学方法、导师、作品；像素差全部为 0 |
| 真实筛选和详情 | 阶段筛选、搜索、空态、重置、slug直达/刷新通过 |
| 真实后台发布联动 | 下架后官网详情不可见，重新发布后恢复 |
| 真实咨询与重试 | 未勾选不能提交；网络失败保持相同请求编号，修改内容生成新编号；六控件禁用；仅真实回执触发成功 |
| 真实管理端跟进 | 原顾问账号看到新 CRM 线索，并保存和重新读取内部备注 |

浏览器报告共 **19 项通过**，无页面异常。可复现脚本：`tooling/bootstrap/verify-orange-unification.playwright.js`；原始报告和截图在忽略目录 `.runtime/unified/orange-browser-report.json` 与 `.runtime/unified/orange-browser/`。

视觉比较只给朋友旧前端的 `/education/courses` 注入从统一服务读取的同一展示数据，以排除数据差异；它仅用于视觉基准。新前端的课程、提交、发布和顾问跟进均访问真实 48081 接口，没有伪造成功响应。测试使用既有独立恢复库，未写原 48080 业务数据。

本轮 `apps/server`、`apps/admin`、`apps/miniapp` 和迁移 SQL 与 `fc320d65` 无差异。其43项后端测试、数据库及原小程序业务回归证据沿用[上一阶段记录](2026-09-14-unified-backend.md)，不冒充本轮重新执行。没有部署生产或执行真实支付。
